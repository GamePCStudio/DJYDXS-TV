<?xml version="1.0" encoding="utf-8"?>
<!--
  设置页：必须可滚动。

  之前这里根布局是 layout_height="wrap_content" 的 LinearLayout，没有 ScrollView：
  条目加起来超过一屏后，超出部分直接被裁掉 —— 遥控器上焦点还能继续往下移到它们，
  但屏幕上永远看不到（v1.25 加了「播放·音频/视频」两组共 9 条后必然触发）。
  这里照 activity_detail.xml 的既有写法包一层 ScrollView，并给底部留出余量，
  保证最后一条（解除授权 / 音频诊断）能完整滚进可视区。
-->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:scrollbarStyle="outsideOverlay">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="20dp"
        android:paddingTop="20dp"
        android:paddingEnd="20dp"
        android:paddingBottom="72dp">

        <TextView
            android:id="@+id/tvSection"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FF1E88E5"
            android:textSize="16sp"
            android:textStyle="bold"
            android:layout_marginTop="8dp" />

        <LinearLayout
            android:id="@+id/lineGroup"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:orientation="vertical" />
    </LinearLayout>
</ScrollView>
