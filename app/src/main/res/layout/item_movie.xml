<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="8dp"
    android:focusable="true"
    android:clickable="true"
    android:descendantFocusability="blocksDescendants">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="6dp">

        <com.supermov.tv.PosterView
            android:id="@+id/ivPic"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scaleType="centerCrop"
            android:background="@drawable/bg_movie_normal" />

        <!-- LAMPA 效果②：白描边焦点框（叠加层，盖在海报之上才看得见）。
             平时 GONE；焦点拿到时 VISIBLE。FrameLayout 子 View 顺序：后声明的在上，
             所以白框压在 ivPic/tvBadge 之上 —— 对齐 LAMPA .card__view::after 的外扩白圈。 -->
        <View
            android:id="@+id/focusFrame"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_focus_ring"
            android:visibility="gone" />

        <!-- 图片内底部：日期 · 片长（渐变底衬） -->
        <TextView
            android:id="@+id/tvBadge"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:background="@drawable/bg_poster_badge"
            android:paddingStart="5dp"
            android:paddingEnd="5dp"
            android:paddingTop="10dp"
            android:paddingBottom="3dp"
            android:textColor="#FFEEEEEE"
            android:textSize="8sp"
            android:maxLines="1"
            android:ellipsize="end" />
    </FrameLayout>

    <TextView
        android:id="@+id/tvName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:textColor="#FFFFFFFF"
        android:textSize="10sp"
        android:maxLines="1"
        android:ellipsize="end" />
</LinearLayout>
