// Top-level build file
plugins {
    // AGP 对齐到 NEXIO media3 fork 自己用的版本（fork build.gradle 里是 8.13.2），
    // 免得 composite build 里并存两套 AGP 行为。
    // 注意：AGP 8.13.2 硬性要求 Gradle >= 8.13（见 gradle-wrapper.properties）。
    id 'com.android.application' version '8.13.2' apply false
}
